#!/bin/sh
# Required to get -lOpenCL linker flag to work
sudo apt-get install ocl-icd-opencl-dev

# Install specific opencl icd driver
# sudo apt-get install intel-opencl-icd
# sudo apt-get install mesa-opencl-icd
